<Window x:Class="ScradaCadeauBonnen_WPF.Betaal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ScradaCadeauBonnen_WPF"
        mc:Ignorable="d"
        Title="Betaal" Height="500" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Content="Scrada" FontSize="24" FontWeight="Bold" 
               HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,0,0,20" />

        <Border Grid.Column="0" Grid.Row="1" Grid.RowSpan="2" Background="#F5F5F5" Margin="0,0,10,0" Padding="15" CornerRadius="5">
            <StackPanel>
                <Label Content="Kies je betaalwijze" FontWeight="SemiBold" Margin="0,0,0,15"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <RadioButton GroupName="Options" Content="Kaart" Margin="0,0,15,0" IsChecked="True"/>
                    <RadioButton GroupName="Options" Content="PayPal" Margin="0,0,15,0"/>
                    <RadioButton GroupName="Options" Content="ApplePay" Margin="0,0,15,0"/>
                </StackPanel>

                <TextBox Text="KaartNr" Padding="5" Margin="0,0,0,10"/>
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBox Text="Datum" Padding="5" Grid.Column="0" Margin="0,0,5,0"/>
                    <TextBox Text="CVC code" Padding="5" Grid.Column="1" Margin="5,0,0,0"/>
                </Grid>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Grid.Row="1" Grid.RowSpan="2" Background="#F5F5F5" Margin="10,0,0,0" Padding="15" CornerRadius="5">
            <DockPanel>
                <Label Content="Jouw selectie" FontWeight="SemiBold" DockPanel.Dock="Top" Margin="0,0,0,10"/>
                <ListBox Name="ItemListBox" DockPanel.Dock="Bottom">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding}" VerticalAlignment="Center" Margin="5"/>
                                <Button Content="❌" Click="RemoveItem_Click" Tag="{Binding}" 
                                        Width="24" Height="24" FontSize="10" Margin="5" Grid.Column="1"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <Button Grid.Column="0" Grid.Row="3" Grid.ColumnSpan="2" Content="Betaal" 
                Click="Betaal_Click" Height="40" Margin="0,20,0,0" Background="#4CAF50" Foreground="White"/>
       
    </Grid>
</Window>